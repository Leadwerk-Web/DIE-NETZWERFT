#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../loose-envify/cli.js" "$@"
else
  exec node  "$basedir/../../../loose-envify/cli.js" "$@"
fi
# cmd-shim-target=/Users/atlas/Downloads/Kimi_Agent_Homepage%20Redesign%20Brief/app/node_modules/.pnpm/react-transition-group@4.4.5_react-dom@19.2.7_react@19.2.7__react@19.2.7/node_modules/loose-envify/cli.js
