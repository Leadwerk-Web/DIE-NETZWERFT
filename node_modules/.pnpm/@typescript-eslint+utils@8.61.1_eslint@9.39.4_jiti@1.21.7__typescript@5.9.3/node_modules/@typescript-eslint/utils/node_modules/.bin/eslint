#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../eslint/bin/eslint.js" "$@"
else
  exec node  "$basedir/../../../../eslint/bin/eslint.js" "$@"
fi
# cmd-shim-target=/Users/atlas/Downloads/Kimi_Agent_Homepage%20Redesign%20Brief/app/node_modules/.pnpm/@typescript-eslint+utils@8.61.1_eslint@9.39.4_jiti@1.21.7__typescript@5.9.3/node_modules/eslint/bin/eslint.js
