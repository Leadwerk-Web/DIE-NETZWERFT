#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../../typescript/bin/tsc" "$@"
else
  exec node  "$basedir/../../../../typescript/bin/tsc" "$@"
fi
# cmd-shim-target=/Users/atlas/Downloads/Kimi_Agent_Homepage%20Redesign%20Brief/app/node_modules/.pnpm/@typescript-eslint+eslint-plugin@8.61.1_@typescript-eslint+parser@8.61.1_eslint@9.39.4__0de5f47a36c2dcbbecd303a2fc982c8d/node_modules/typescript/bin/tsc
